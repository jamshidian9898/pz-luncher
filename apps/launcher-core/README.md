# launcher-core

This folder contains the foundation layer for the launcher core service.

It defines the `LauncherCore` interface and state machine for bootstrapping the launcher flow:

- `JoinServer()`
- `ResolveManifest()`
- `ResolvePackages()`
- `DownloadMissing()`
- `PrepareProfile()`
- `Launch()`
