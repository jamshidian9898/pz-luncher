# downloader — DEPRECATED

> **This package is superseded by [`libs/download`](../download/).  
> Do not add new code here. Use `libs/download` for all download queue and state management.**

---

This placeholder was the original download abstraction (resume, chunking, session management).  
It has been replaced by the RFC-0032 implementation in `libs/download/queue.go`.
